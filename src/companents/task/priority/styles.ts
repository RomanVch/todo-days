export const styles = (color: string) => ({
  priority: {
    width: 5,
    height: 40,
    background: `${color}`,
    borderRadius: 4,
    border: 0,
    margin: 0,
    marginRight: "14px",
  },
});
